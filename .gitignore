node_modules/

# Build output
build/
dist/
.react-router/

# Cloudflare
.wrangler/
.dev.vars
worker-configuration.d.ts

# Editor and OS
.DS_Store
Thumbs.db
*.log
npm-debug.log*

# Full-resolution photo originals and raw reference material. Only the derived
# files in public/photos are committed, so the repo stays small but originals
# remain reprocessable locally.
#
# Leading slashes matter: an unanchored "photos/" would also match
# public/photos/, which is the one photo directory that must be committed.
/photos-source/
/photos/

# Design review captures, regenerated by scripts/shoot.mjs
/screenshots/

# Local env
.env
.env.local
.env.*.local
